import React from 'react';

export const DeletedNote = () => {
    return (
        <div 
                        className="note-form-delete"
                        onClick={ handleDelete }
        >
            
        </div>
    )
}
